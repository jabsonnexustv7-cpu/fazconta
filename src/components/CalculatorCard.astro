---
interface Props {
  title: string;
  description: string;
  href: string;
  badge?: string;
  icon?: string;
}

const { title, description, href, badge = 'Calculadora', icon = '◈' } = Astro.props;
---

<a class="calculator-card" href={href}>
  <div class="card-top">
    <span class="card-icon" aria-hidden="true">{icon}</span>
    <span class="badge">{badge}</span>
  </div>
  <h3>{title}</h3>
  <p>{description}</p>
  <strong class="card-cta">Calcular agora →</strong>
</a>
