---
interface Props {
  position?: 'top' | 'middle' | 'bottom';
}

const { position = 'middle' } = Astro.props;
---
<div class={`ad-slot ad-slot--${position}`} role="complementary" aria-label="Espaço reservado para anúncio">
  Espaço reservado para anúncio
</div>
